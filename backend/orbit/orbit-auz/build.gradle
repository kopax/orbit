dependencies {
    compile project(":orbit-domain")
    compile project(":orbit-exception")
    compile group: 'commons-io', name: 'commons-io', version: '2.5'
    compile "org.apache.shiro:shiro-spring-boot-starter:${rootProject.shiroVersion}"
    compile "net.pusuo:patchca:0.5.0"
}